class Tag {
    constructor(id, name, colour, isActive=true) {
        this.id = id;
        this.name = name;
        this.colour = colour;
        this.isActive = isActive;
    }
}

export default Tag;